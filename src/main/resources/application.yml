server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/university
    username: manager
    password: 4518864
  jpa:
    generate-ddl: true
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL94Dialect